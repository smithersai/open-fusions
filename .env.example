# open-fusions reads model API keys from the environment.
# Default routing is through OpenRouter (one key reaches the whole model catalog).
OPENROUTER_API_KEY=
# Optional native providers (used when a panel member targets them directly):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
